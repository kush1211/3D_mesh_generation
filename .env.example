# Gemini API key (free-tier). Read automatically by langchain-google-genai.
GEMINI_API_KEY=your-gemini-api-key-here

# Context7 MCP API key (OPTIONAL — higher rate limits; works without it).
# Format: ctx7sk_...  Generate at https://context7.com/dashboard
CONTEXT7_API_KEY=
